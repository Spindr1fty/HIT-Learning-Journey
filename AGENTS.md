# AGENTS.md — 项目说明（供 AI 助手阅读）

## 项目概述

本仓库是作者作为交换生在哈尔滨工业大学（HIT）学习**机器人/嵌入式开发**的记录仓库，
内容包括学习笔记、代码和项目实践。学习主线：

- **ROS2**（机器人开发）：C++ / Python
- **嵌入式**：C 语言、STM32
- **Java**：基础入门与进阶

## 语言约定

- 笔记、注释、文档、commit message 一律使用**简体中文**
- 代码中的标识符、文件名、命令保持英文
- README 已有英文介绍部分，新增内容可中英混排，但以中文为主

## 目录约定（建议）

当前仓库尚未建立内容目录，新增内容时按主题归类，避免堆在根目录：

```
notes/        # 学习笔记（Markdown）
code/         # 示例代码 / 练习代码
  ros2/       # ROS2 相关
  embedded/   # C / STM32 相关
  java/       # Java 相关
projects/     # 项目实践
```

若实际内容较少，也可先按 `ros2/`、`embedded/`、`java/` 主题建目录，保持结构扁平。

## 技术要点

- 嵌入式开发：注意 C 语言与硬件相关的细节（寄存器、指针、内存布局）
- ROS2：工作空间构建（`colcon build`）、节点/话题/服务编程
- 代码与笔记配套时，笔记中注明代码所在路径

## 禁止事项

- **不要修改或删除 `.reasonix/`** —— 它是 Reasonix 工具内部状态，已被 `.gitignore` 忽略
- 不要提交 `node_modules/`（已忽略）、临时文件（`*.tmp`、`*.log`）和个人环境文件（`.env`）
- 不要修改 `LICENSE`、`.gitignore` 的现有规则，除非用户明确要求

## Git 约定

- commit message 用中文，简洁描述改动（参考现有历史：如「readme编写」「文件夹初始化」）
- 提交前确认工作区无多余文件（`git status` 检查）
